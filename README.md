# AI_Poject_MountainCar
The only requirements to run `MountainCar.py` are `gym` and `numpy`.
```
pip install gym
pip install numpy
```
to train and see the performance, simply run:
```
python MountainCar.py
```
